import 'package:flutter/material.dart';

class AppPadding {
  const AppPadding._();

  ///horizontal:10
  static const pagePaddingLow = EdgeInsets.symmetric(horizontal: 10);

  ///vertical: 20
  static const myButtonTextPadding = EdgeInsets.symmetric(vertical: 20);

  ///top: 10
  static const paddingTopLow = EdgeInsets.only(top: 10);

  /// right: 10
  static const paddingRightLow = EdgeInsets.only(right: 10);

  ///EdgeInsets.all(10)
  static const paddingContainerButton = EdgeInsets.all(10);
  ///EdgeInsets.only(bottom: 10)
  static const paddingBottomLow = EdgeInsets.only(bottom: 10);
  ///EdgeInsets.only(left: 5)
  static const paddingLeft5 = EdgeInsets.only(left: 5);
  ///EdgeInsets.only(right: 5)
  static const paddingRight5 = EdgeInsets.only(right: 5);
}
